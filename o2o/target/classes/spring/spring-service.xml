<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd">
	<!-- configure all the types under service package -->
	<!-- 1. configure database parameters：${url}, classpath = src/main/resources -->
    <context:component-scan base-package="com.immoc.service"/>

    <!-- configure transaction manager -->
    <bean id="transactionManager"
        class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <!--insert database pool-->
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- configure annotaion-driven events -->
    <tx:annotation-driven transaction-manager="transactionManager"/>
</beans>